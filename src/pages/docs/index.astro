---
import BaseLayout from "../../layouts/BaseLayout.astro";

const signalsResponse = JSON.stringify({
  count: 2,
  signals: [{
    id: 195,
    asset: null,
    signal_type: "regulatory",
    direction: "neutral",
    urgency: "medium",
    summary: "Senate Banking Committee scheduled to vote on the Clarity Act crypto bill.",
    confidence: 0.90,
    action_hint: "monitor",
    corroborated: false,
    score: 0.90,
    trade_quality_score: 0.61,
    trade_quality_band: "B",
    expected_move_pct: null,
    regime_at_signal: "risk-off",
    timestamp: 1778555605,
    expires_at: 1778570005,
    disclaimer: "Analytical data only. Not financial advice."
  }],
  market_opens: {
    tokyo: {BTC: 81150.00, ETH: 3200.50, SOL: 96.30, timestamp: 1778539200},
    london: {BTC: 81201.50, ETH: 3205.00, SOL: 96.48, timestamp: 1778553600},
    new_york: {BTC: 81262.50, ETH: 3210.00, SOL: 96.55, timestamp: 1778560800}
  }
}, null, 2);

const proFields = JSON.stringify({
  sources: ["news", "whale"],
  source: "news",
  channel: "google_crypto"
}, null, 2);

const levelsResponse = JSON.stringify({
  coin: "BTC",
  current_price: 81262.50,
  nearest_resistance: 81450.00,
  nearest_support: 80871.00,
  all_resistance: [81450.00, 81800.00, 82200.00, 82500.00, 83000.00],
  all_support: [80871.00, 80500.00, 80000.00, 79500.00, 79000.00]
}, null, 2);

const marketOpensResponse = JSON.stringify({
  market_opens: {
    tokyo: {BTC: 81150.00, ETH: 3200.50, SOL: 96.30, timestamp: 1778539200},
    shanghai: {BTC: 81180.00, ETH: 3202.00, SOL: 96.35, timestamp: 1778542800},
    hong_kong: {BTC: 81190.00, ETH: 3203.50, SOL: 96.40, timestamp: 1778542800},
    london: {BTC: 81201.50, ETH: 3205.00, SOL: 96.48, timestamp: 1778553600},
    new_york: {BTC: 81262.50, ETH: 3210.00, SOL: 96.55, timestamp: 1778560800}
  }
}, null, 2);

const registerResponse = JSON.stringify({
  api_key: "intel_sk_a1b2c3d4e5f6...",
  key_prefix: "intel_sk",
  tier: "free",
  message: "Save your API key - it will not be shown again."
}, null, 2);

const loginResponse = JSON.stringify({
  token: "eyJhbGciOiJIUzI1NiIs...",
  tier: "free",
  api_keys: [{id: 1, prefix: "intel_sk_a1b2", name: "default", created_at: 1778555605}]
}, null, 2);

const webhookCreateResponse = JSON.stringify({
  id: 1,
  url: "https://your-server.com/hooks/n0brains",
  secret: "a1b2c3d4...",
  events: "signal.created",
  note: "Use X-Intel-Signature header to verify deliveries. Secret is shown only once."
}, null, 2);

const webhookDeliveryResponse = JSON.stringify({
  event: "signal.created",
  timestamp: 1778555605,
  data: {
    id: 195,
    asset: "BTC",
    signal_type: "whale",
    direction: "bullish",
    urgency: "high",
    summary: "Strategy buys 535 BTC for $43M",
    confidence: 0.95,
    score: 1.42,
    trade_quality_band: "A",
    regime_at_signal: "risk-on"
  }
}, null, 2);

const narrativeResponse = JSON.stringify({
  coin: "BTC",
  narrative_strength: 0.74,
  narrative_velocity: 3.2,
  narrative_decay_rate: 0.18,
  manipulation_probability: 0.12,
  dominant_direction: "bullish",
  direction_conviction: 0.81,
  source_diversity: 4,
  signal_count_1h: 7,
  signal_count_4h: 19,
  signal_count_24h: 42,
  disclaimer: "Analytical data only. Not financial advice."
}, null, 2);

const manipulationResponse = JSON.stringify({
  coin: "ETH",
  manipulation_score: 0.34,
  social_burst_score: 0.20,
  coordinated_pump_score: 0.50,
  liquidation_cascade_score: 0.10,
  flags: ["coordinated_pump"],
  disclaimer: "Analytical data only. Not financial advice."
}, null, 2);

const trustResponse = JSON.stringify({
  asset: "PEPE",
  address: "0x6982508145454ce325ddbe47a25d4ec3d2311933",
  chain_id: "1",
  trust_score: 0.82,
  rug_probability: 0.18,
  contract_risk: 0.10,
  insider_pct: 0.04,
  is_honeypot: false,
  is_mintable: false,
  sell_tax: 0.0,
  low_liquidity: false,
  liquidity_usd: 4820000,
  flags: [],
  data_source: "goplus",
  computed_at: 1778555605,
  disclaimer: "Analytical data only. Not financial advice."
}, null, 2);

const similarResponse = JSON.stringify([
  {
    id: 182,
    similarity: 0.97,
    summary: "BTC whale | bullish | high | Strategy loads 420 BTC at open",
    metadata: {asset: "BTC", signal_type: "whale", direction: "bullish", urgency: "high", score: 1.38, timestamp: 1778551200}
  },
  {
    id: 178,
    similarity: 0.94,
    summary: "BTC whale | bullish | medium | Institutional wallet accumulates 310 BTC",
    metadata: {asset: "BTC", signal_type: "whale", direction: "bullish", urgency: "medium", score: 1.10, timestamp: 1778548800}
  }
], null, 2);

const errorResponses = `// 401 Unauthorized
{"detail": "Invalid API key"}

// 429 Rate Limited
{"detail": "Rate limit exceeded (60 req/min for free tier). Upgrade at https://n0brains.com"}

// 404 Not Found
{"detail": "Signal not found"}

// 403 Forbidden
{"detail": "WebSocket requires Pro tier"}`;

const websocketExample = `wss://api.n0brains.com/stream?api_key=intel_sk_...

Signal received:
{"type": "signal", "data": {"id": 195, "asset": "BTC", "signal_type": "whale", "direction": "bullish", "urgency": "high", "summary": "Strategy buys 535 BTC for $43M", "confidence": 0.95, "score": 1.42, "trade_quality_band": "A", "regime_at_signal": "risk-on"}}

Ping/pong:
send: "ping"
recv: {"type": "pong", "timestamp": 1778555605}`;

const macroPulseResponse = JSON.stringify({
  generated_at: "2026-05-13T07:00:00+00:00",
  window_days: 7,
  horizon: "week",
  regime: "risk-off",
  regime_reason: "Hotter-than-expected CPI/PPI driving inflation fears and heavy long de-leveraging.",
  btc: {
    bias: "bearish",
    conviction: 0.85,
    reason: "Macro bearishness triggered by PPI surge and heavy long liquidations.",
    key_levels: "$80,000 (psychological), $76,500 (structural)",
    invalidation: "Retail Sales print materially below forecast, sparking risk-on rotation."
  },
  eth: {
    bias: "bearish",
    conviction: 0.90,
    reason: "Extreme long squeeze risk; 97% long liquidations on key venues.",
    key_levels: "$3,200 (immediate support), $2,850 (major pivot)",
    invalidation: "Unexpected dovish turn from new Fed chair."
  },
  calendar_risks: [
    "Core Retail Sales m/m (2026-05-14) — potential further downside if hot",
    "Fed Chair Nomination Vote (2026-05-13) — policy uncertainty"
  ]
}, null, 2);
---
<BaseLayout title="API Docs" description="n0brains Signals API documentation — endpoints, authentication, rate limits, and response formats.">
  <script type="application/ld+json">{JSON.stringify({
    "@context": "https://schema.org",
    "@type": "WebAPI",
    "name": "n0brains API",
    "description": "REST + WebSocket + Webhook API delivering scored crypto intelligence signals — whales, funding, liquidations, on-chain flows, macro bias, congress trades, X (Twitter) curated feed, and more. Each signal includes direction, urgency, confidence, and quality band. Analytical data only, not financial advice.",
    "url": "https://n0brains.com/docs/",
    "documentation": "https://n0brains.com/docs/",
    "termsOfService": "https://n0brains.com/terms",
    "provider": {
      "@type": "Organization",
      "name": "n0brains",
      "url": "https://n0brains.com"
    },
    "offers": [
      { "@type": "Offer", "price": "0", "priceCurrency": "USD", "description": "Free tier — 60 req/min, REST only, 15-min delay" },
      { "@type": "Offer", "price": "19.99", "priceCurrency": "USD", "description": "Pro tier — 600 req/min, real-time, REST + WebSocket + Webhooks" }
    ],
    "potentialAction": {
      "@type": "SearchAction",
      "target": "https://api.n0brains.com/signals?asset={asset}&signal_type={type}",
      "query-input": "required name=asset"
    }
  })}</script>
  <section class="docs-page">
    <h1>API Reference</h1>
    <p class="docs-subtitle">Base URL: <code>https://api.n0brains.com</code> &middot; All responses are JSON &middot; Timestamps are Unix epoch (seconds)</p>

    <div class="card">
      <h2 id="quickstart">Quick Start</h2>
      <ol>
        <li><a href="/signup">Create an account</a> — get your API key instantly</li>
        <li>Set the header <code>X-API-Key</code> on every request</li>
        <li>Hit <code>GET /signals</code> — you are done</li>
      </ol>
      <pre><code>curl -H "X-API-Key: intel_sk_your_key" https://api.n0brains.com/signals</code></pre>
      <p class="docs-tip">Running an OpenClaw agent? Just hand it your API key and the base URL — it will figure out the rest. Tell it: <em>"Use the n0brains API at https://api.n0brains.com with X-API-Key intel_sk_your_key"</em> and let it trade. More — including an optional skill file — on the <a href="/agents/">agents page</a>.</p>
    </div>

    <div class="card">
      <h2 id="auth">Authentication</h2>
      <p>All authenticated endpoints require an API key via the <code>X-API-Key</code> header. Keys are generated on registration and can be rotated via <code>POST /auth/api-keys</code>.</p>
      <p>Login also returns a JWT token usable via <code>Authorization: Bearer</code>.</p>
      <table>
        <thead><tr><th>Method</th><th>Header</th><th>Example</th></tr></thead>
        <tbody>
          <tr><td>API Key</td><td><code>X-API-Key</code></td><td><code>intel_sk_a1b2c3...</code></td></tr>
          <tr><td>JWT Token</td><td><code>Authorization</code></td><td><code>Bearer eyJhbGci...</code></td></tr>
        </tbody>
      </table>
    </div>

    <div class="card">
      <h2 id="rate-limits">Rate Limits</h2>
      <table>
        <thead><tr><th>Tier</th><th>Requests/min</th><th>Signal Delay</th><th>Access</th></tr></thead>
        <tbody>
          <tr><td>Free</td><td>60</td><td>15 minutes</td><td>REST API only, limited fields</td></tr>
          <tr><td>Pro</td><td>600</td><td>Real-time</td><td>REST + WebSocket + Webhooks, full fields</td></tr>
        </tbody>
      </table>
      <p>Rate limit exceeded returns <code>429</code> with a message pointing to the upgrade page.</p>
    </div>

    <div class="card">
      <h2 id="endpoints">Endpoints</h2>
      <table>
        <thead><tr><th>Method</th><th>Path</th><th>Auth</th><th>Description</th></tr></thead>
        <tbody>
          <tr><td><span class="method method-get">GET</span></td><td><code>/health</code></td><td>None</td><td>Liveness check</td></tr>
          <tr><td><span class="method method-get">GET</span></td><td><code>/signals</code></td><td>API Key</td><td>List active signals (filterable)</td></tr>
          <tr><td><span class="method method-get">GET</span></td><td><code>/signals/{'{id}'}</code></td><td>API Key</td><td>Single signal detail</td></tr>
          <tr><td><span class="method method-get">GET</span></td><td><code>/signals/similar</code></td><td>Pro</td><td>Semantic nearest-neighbor search</td></tr>
          <tr><td><span class="method method-get">GET</span></td><td><code>/narrative/{'{coin}'}</code></td><td>Pro</td><td>Narrative heatmap for asset</td></tr>
          <tr><td><span class="method method-get">GET</span></td><td><code>/manipulation/{'{coin}'}</code></td><td>Pro</td><td>Manipulation score for asset</td></tr>
          <tr><td><span class="method method-get">GET</span></td><td><code>/trust/{'{coin}'}</code></td><td>Pro</td><td>Trust &amp; scam score for token</td></tr>
          <tr><td><span class="method method-get">GET</span></td><td><code>/levels/{'{coin}'}</code></td><td>API Key</td><td>S/R levels from Hyperliquid</td></tr>
          <tr><td><span class="method method-get">GET</span></td><td><code>/market-opens</code></td><td>API Key</td><td>Session opening prices</td></tr>
          <tr><td><span class="method method-get">GET</span></td><td><code>/macro</code></td><td>API Key</td><td>Macro Pulse — latest BTC + ETH directional bias</td></tr>
          <tr><td><span class="method method-get">GET</span></td><td><code>/macro?history=N</code></td><td>Pro</td><td>Macro Pulse historical snapshots (last N reports, max 30)</td></tr>
          <tr><td><span class="method method-get">GET</span></td><td><code>/performance</code></td><td>None</td><td>Public win rate / PnL stats</td></tr>
          <tr><td><span class="method method-post">POST</span></td><td><code>/auth/register</code></td><td>None</td><td>Create account</td></tr>
          <tr><td><span class="method method-post">POST</span></td><td><code>/auth/login</code></td><td>None</td><td>Login</td></tr>
          <tr><td><span class="method method-get">GET</span></td><td><code>/auth/me</code></td><td>API Key</td><td>User profile</td></tr>
          <tr><td><span class="method method-post">POST</span></td><td><code>/auth/api-keys</code></td><td>API Key</td><td>Generate new API key</td></tr>
          <tr><td><span class="method method-del">DEL</span></td><td><code>/auth/api-keys/{'{id}'}</code></td><td>API Key</td><td>Revoke API key</td></tr>
          <tr><td><span class="method method-post">POST</span></td><td><code>/billing/checkout</code></td><td>API Key</td><td>Stripe checkout session</td></tr>
          <tr><td><span class="method method-post">POST</span></td><td><code>/billing/portal</code></td><td>API Key</td><td>Stripe customer portal</td></tr>
          <tr><td><span class="method method-post">POST</span></td><td><code>/webhooks</code></td><td>Pro</td><td>Register webhook</td></tr>
          <tr><td><span class="method method-del">DEL</span></td><td><code>/webhooks/{'{id}'}</code></td><td>Pro</td><td>Remove webhook</td></tr>
          <tr><td><span class="method method-ws">WS</span></td><td><code>/stream</code></td><td>Pro</td><td>Real-time signal stream</td></tr>
        </tbody>
      </table>
    </div>

    <div class="card">
      <h2 id="signals-endpoint">GET /signals</h2>
      <p>Returns active signals, optionally filtered. Free tier receives signals with a 15-minute delay.</p>
      <h3>Query Parameters</h3>
      <table>
        <thead><tr><th>Param</th><th>Type</th><th>Description</th></tr></thead>
        <tbody>
          <tr><td><code>asset</code></td><td>string</td><td>Filter by ticker (e.g. BTC)</td></tr>
          <tr><td><code>signal_type</code></td><td>string</td><td>regulatory, whale, sentiment, price, hack, listing, macro, macro_pulse, liquidation, funding, other</td></tr>
          <tr><td><code>direction</code></td><td>string</td><td>bullish, bearish, neutral</td></tr>
          <tr><td><code>urgency</code></td><td>string</td><td>high, medium, low</td></tr>
          <tr><td><code>limit</code></td><td>int</td><td>1-200 (default 50)</td></tr>
          <tr><td><code>offset</code></td><td>int</td><td>Pagination offset (default 0)</td></tr>
          <tr><td><code>include_opens</code></td><td>bool</td><td>Include market opening prices (default true)</td></tr>
          <tr><td><code>min_confidence</code></td><td>float</td><td>Minimum confidence score (0.0–1.0, default 0.0)</td></tr>
          <tr><td><code>min_score</code></td><td>float</td><td>Minimum signal score (default 0.0)</td></tr>
          <tr><td><code>band</code></td><td>string</td><td>Filter by trade quality band: A+, A, B, C, D</td></tr>
          <tr><td><code>min_trade_quality</code></td><td>float</td><td>Minimum trade quality score (0.0–1.0, default 0.0)</td></tr>
        </tbody>
      </table>
      <h3>Response - Free Tier</h3>
      <pre><code set:html={signalsResponse} /></pre>
      <h3>Pro Tier Adds</h3>
      <p>Each signal object includes these additional fields:</p>
      <pre><code set:html={proFields} /></pre>
    </div>

    <div class="card">
      <h2 id="signal-fields">Signal Fields</h2>
      <table>
        <thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>
        <tbody>
          <tr><td><code>id</code></td><td>int</td><td>Unique signal ID</td></tr>
          <tr><td><code>asset</code></td><td>string|null</td><td>Ticker symbol or null if market-wide</td></tr>
          <tr><td><code>signal_type</code></td><td>string</td><td>regulatory, whale, sentiment, price, hack, listing, macro, macro_pulse, liquidation, funding, other</td></tr>
          <tr><td><code>direction</code></td><td>string</td><td>bullish, bearish, neutral</td></tr>
          <tr><td><code>urgency</code></td><td>string</td><td>high, medium, low</td></tr>
          <tr><td><code>summary</code></td><td>string</td><td>Max 100 char description</td></tr>
          <tr><td><code>confidence</code></td><td>float</td><td>0.0-1.0, LLM-assessed confidence</td></tr>
          <tr><td><code>action_hint</code></td><td>string</td><td>trade_signal, alert, monitor, ignore</td></tr>
          <tr><td><code>corroborated</code></td><td>bool</td><td>True if multiple sources confirm</td></tr>
          <tr><td><code>score</code></td><td>float</td><td>source_weight x urgency_weight x confidence</td></tr>
          <tr><td><code>timestamp</code></td><td>int</td><td>Unix epoch (seconds)</td></tr>
          <tr><td><code>expires_at</code></td><td>int</td><td>When this signal expires</td></tr>
          <tr><td><code>sources</code></td><td>array</td><td>Pro only - Source names that contributed</td></tr>
          <tr><td><code>source</code></td><td>string</td><td>Pro only - Primary source name</td></tr>
          <tr><td><code>channel</code></td><td>string</td><td>Pro only - Specific channel/feed</td></tr>
          <tr><td><code>trade_quality_score</code></td><td>float</td><td>0.0–1.0 composite signal quality (regime + confidence + corroboration)</td></tr>
          <tr><td><code>trade_quality_band</code></td><td>string</td><td>A+/A/B/C/D quality band — A+ ≥ 0.85, A ≥ 0.70, B ≥ 0.55, C ≥ 0.40</td></tr>
          <tr><td><code>expected_move_pct</code></td><td>float|null</td><td>Estimated move size as % of price based on nearest S/R delta + ATR</td></tr>
          <tr><td><code>regime_at_signal</code></td><td>string</td><td>Market regime when signal was generated: risk-on, risk-off, range, squeeze</td></tr>
          <tr><td><code>manipulation_score</code></td><td>float</td><td>0.0–1.0 coordination/manipulation probability at signal creation time</td></tr>
          <tr><td><code>wallet_type</code></td><td>string|null</td><td>Whale signal only — vc_institutional, smart_whale, market_maker, mev_arb, sniper, etc.</td></tr>
          <tr><td><code>smart_money_score</code></td><td>float|null</td><td>Whale signal only — credibility of originating wallet (0.0–1.0)</td></tr>
          <tr><td><code>trust_score</code></td><td>float|null</td><td>Launchpad/token signals — composite contract trust (0.0–1.0)</td></tr>
          <tr><td><code>rug_probability</code></td><td>float|null</td><td>Launchpad/token signals — estimated rug/scam probability (0.0–1.0)</td></tr>
          <tr><td><code>is_honeypot</code></td><td>bool|null</td><td>Launchpad/token signals — GoPlus honeypot flag</td></tr>
        </tbody>
      </table>
    </div>

    <div class="card">
      <h2 id="levels">GET /levels/{'{coin}'}</h2>
      <p>Returns nearest support/resistance levels from the Hyperliquid order book.</p>
      <pre><code set:html={levelsResponse} /></pre>
    </div>

    <div class="card">
      <h2 id="market-opens">GET /market-opens</h2>
      <p>Opening prices for BTC, ETH, and SOL at each major trading session.</p>
      <pre><code set:html={marketOpensResponse} /></pre>
    </div>

    <div class="card">
      <h2 id="macro-pulse">GET /macro</h2>
      <p><strong>Macro Pulse</strong> — our daily directional bias for BTC and ETH. Generated at 07:00 UTC daily (with a deeper 30-day review every Sunday 22:00 UTC). Synthesises the upcoming USD high-impact macro calendar (FOMC, CPI, NFP, PPI, Retail Sales) with the last 7 days of intel signals into a single regime call plus per-asset bias, conviction, key levels, and invalidation.</p>
      <p>Free tier: latest report only, generated_at + regime + bias/conviction. Pro tier: full payload including reasoning, key levels, invalidation, and calendar_risks. Add <code>?history=N</code> (Pro, max 30) to include the last N snapshots alongside the current report.</p>
      <pre><code set:html={macroPulseResponse} /></pre>
      <h3>Field Reference</h3>
      <table>
        <thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>
        <tbody>
          <tr><td><code>regime</code></td><td>string</td><td>risk-on, risk-off, range, squeeze</td></tr>
          <tr><td><code>regime_reason</code></td><td>string</td><td>≤120 char rationale</td></tr>
          <tr><td><code>btc.bias</code> / <code>eth.bias</code></td><td>string</td><td>bullish, bearish, neutral</td></tr>
          <tr><td><code>btc.conviction</code> / <code>eth.conviction</code></td><td>float</td><td>0.0–1.0. ≥0.7 = very high. ≥0.85 = veto-grade contradictions.</td></tr>
          <tr><td><code>btc.reason</code> / <code>eth.reason</code></td><td>string</td><td>Primary driver, ≤140 chars</td></tr>
          <tr><td><code>btc.key_levels</code> / <code>eth.key_levels</code></td><td>string</td><td>Relevant macro pivots</td></tr>
          <tr><td><code>btc.invalidation</code> / <code>eth.invalidation</code></td><td>string</td><td>What flips the call</td></tr>
          <tr><td><code>calendar_risks</code></td><td>array</td><td>Top 1–3 upcoming events that could flip bias</td></tr>
          <tr><td><code>window_days</code></td><td>int</td><td>Lookback window (7 daily / 30 weekly)</td></tr>
          <tr><td><code>horizon</code></td><td>string</td><td>Forward horizon label (e.g. "week", "next two weeks")</td></tr>
          <tr><td><code>generated_at</code></td><td>string</td><td>ISO-8601 UTC. Stale after 36h — clients should refetch.</td></tr>
        </tbody>
      </table>
    </div>

    <div class="card">
      <h2 id="auth-endpoints">Auth Endpoints</h2>
      <h3>POST /auth/register</h3>
      <p>Create a new account. Returns an API key immediately.</p>
      <pre><code set:html={registerResponse} /></pre>
      <h3>POST /auth/login</h3>
      <pre><code set:html={loginResponse} /></pre>
    </div>

    <div class="card">
      <h2 id="websocket">WebSocket /stream</h2>
      <p><em>Pro tier only.</em> Connect to receive signals in real-time.</p>
      <pre><code set:html={websocketExample} /></pre>
    </div>

    <div class="card">
      <h2 id="webhooks">Webhooks</h2>
      <p><em>Pro tier only.</em> Register a URL to receive POST deliveries for new signals.</p>
      <h3>POST /webhooks</h3>
      <pre><code set:html={webhookCreateResponse} /></pre>
      <h3>Delivery Format</h3>
      <p>POST to your URL with header <code>X-Intel-Signature</code>:</p>
      <pre><code set:html={webhookDeliveryResponse} /></pre>
    </div>

    <div class="card">
      <h2 id="sources">Data Sources and Scoring</h2>
      <p>The pipeline ingests from the watchers listed below. Each source carries a weight that multiplies into the final signal score — so what you read isn't a raw feed dump, it's one corroborated, ranked number.</p>
      <p class="formula"><code>score = source_weight × urgency_weight × confidence_decayed</code></p>
      <p>Urgency weights: <code>low: 1.0</code>, <code>medium: 2.0</code>, <code>high: 3.0</code></p>
      <p><strong>Confidence decay:</strong> confidence degrades exponentially with age. <code>confidence_decayed = confidence₀ × exp(−age / τ)</code> where τ = half-life / ln(2) per signal type (liquidation: ~43min, hack/sentiment/listing: ~2.9h, whale/price/funding: ~8.7h, regulatory: ~5.8h, macro: ~11.5h, macro_pulse: ~34.6h). A stale signal has a lower score than a fresh one of identical quality.</p>
      <p><strong>Source weight self-tuning:</strong> weights are EMA-adjusted daily from tracked outcome win rates. Sources that fire correctly get boosted up to 1.4×; sources that underperform are dampened to 0.6×. Minimum 10 outcomes before adjustment.</p>

      <h3>Active Watchers</h3>
      <ol class="source-list">
        <li><strong>Telegram</strong> - WatcherGuru, ZachXBT Investigations, yoyodexhaber, sondakikav</li>
        <li><strong>News RSS</strong> - 15 feeds (Reuters, AP, BBC, CNBC, Bloomberg, CoinDesk, CoinTelegraph, Decrypt, Google News, etc.)</li>
        <li><strong>Government RSS</strong> - 11 feeds (White House, SEC, CFTC, Fed, Kremlin, Xinhua, TASS, EU, etc.)</li>
        <li><strong>CoinMarketCap</strong> - Price alerts, trending coins</li>
        <li><strong>Macro</strong> - Fear and Greed Index, DeFi TVL</li>
        <li><strong>Binance</strong> - Funding rates, liquidation cascades (WebSocket)</li>
        <li><strong>Market Opens</strong> - BTC/ETH/SOL prices at Tokyo, Shanghai, Hong Kong, London, NY session opens</li>
        <li><strong>Launchpad</strong> - Pump.fun WebSocket, Raydium new pairs, Binance/Bybit new listings</li>
        <li><strong>Smart Money</strong> - Blockchair ETH address dashboards, blockchain.com BTC, burn tracking</li>
        <li><strong>Whale Tracking</strong> - Hyperliquid leaderboard (ETH/BTC on-chain), SOL whale wallets via Helius (exchanges, funds, validators — real-time accountSubscribe + REST polling)</li>
        <li><strong>L/S Ratios</strong> - Bybit, Binance, Gate.io positioning data</li>
        <li><strong>S/R Levels</strong> - Real-time support/resistance from Hyperliquid order book</li>
        <li><strong>Macro Pulse</strong> - Daily BTC + ETH directional bias from USD high-impact calendar (FOMC, CPI, NFP, PPI) synthesised with 7d intel signals. Sunday weekly 30d deep-dive.</li>
        <li><strong>Congress Trades</strong> - CapitolExposed API — every crypto trade disclosed by US Congress members. Bitcoin ETFs (IBIT, FBTC, BITB), crypto equities (COIN, MSTR), direct BTC/ETH. Conflict-scored. 34 tickers tracked.</li>
        <li><strong>X (Twitter) API</strong> - Curated 11-account feed via official X API v2. Onchain primary (@lookonchain, @spotonchain, @arkham, @whale_alert), official exchange announcements (@binance, @coinbase, @CoinbaseAssets, @upbit_global), and newswire-speed macro (@DeItaone, @FirstSquawk, @DBdaily). 20s polling, retweets and replies filtered.</li>
      </ol>

      <h3>Source Weights</h3>
      <table>
        <thead><tr><th>Source</th><th>Weight</th><th>Rationale</th></tr></thead>
        <tbody>
          <tr><td>gov_news</td><td>1.3</td><td>Market-moving regulatory/policy events</td></tr>
          <tr><td>congress_trades:crypto</td><td>1.3</td><td>Verified congressional crypto disclosures</td></tr>
          <tr><td>congress_trades:crypto_conflict</td><td>1.5</td><td>Crypto trade + conflict of interest</td></tr>
          <tr><td>congress_trades:high_conflict</td><td>1.2</td><td>High-conflict congressional trade (any asset)</td></tr>
          <tr><td>smart_money</td><td>1.2</td><td>On-chain address tracking</td></tr>
          <tr><td>telegram:investigations</td><td>1.2</td><td>High-signal hack/exploit intel</td></tr>
          <tr><td>x:lookonchain</td><td>1.3</td><td>Onchain wallet moves with tx hashes</td></tr>
          <tr><td>x:binance / x:coinbase</td><td>1.3</td><td>Official exchange listing announcements</td></tr>
          <tr><td>x:DeItaone</td><td>1.2</td><td>Newswire-speed macro headlines</td></tr>
          <tr><td>x:spotonchain / x:arkham</td><td>1.2</td><td>Onchain analytics, labelled wallets</td></tr>
          <tr><td>x:CoinbaseAssets</td><td>1.2</td><td>Coinbase listing roadmap leaks</td></tr>
          <tr><td>x:FirstSquawk</td><td>1.1</td><td>Headline wire, fast macro</td></tr>
          <tr><td>x:upbit_global</td><td>1.1</td><td>Korea listings, kimchi premium catalyst</td></tr>
          <tr><td>x:DBdaily</td><td>0.9</td><td>Corporate adoption tracking</td></tr>
          <tr><td>x:whale_alert</td><td>0.7</td><td>Auto-bot, mostly exchange↔cold storage noise</td></tr>
          <tr><td>whale_sol</td><td>1.0</td><td>SOL whale tracking</td></tr>
          <tr><td>binance_funding</td><td>1.0</td><td>Primary exchange data</td></tr>
          <tr><td>binance_liq</td><td>1.0</td><td>Liquidation data</td></tr>
          <tr><td>cmc_price</td><td>0.9</td><td>CoinMarketCap price alerts</td></tr>
          <tr><td>telegram:WatcherGuru</td><td>0.9</td><td>Fast breaking news</td></tr>
          <tr><td>rss:trumpstruth</td><td>0.9</td><td>Market-moving but noisy</td></tr>
          <tr><td>rss:coindesk</td><td>0.8</td><td>Established outlet</td></tr>
          <tr><td>launchpad</td><td>0.8</td><td>New token launches</td></tr>
          <tr><td>rss:cointelegraph</td><td>0.7</td><td>General crypto news</td></tr>
          <tr><td>rss:decrypt</td><td>0.7</td><td>General crypto news</td></tr>
          <tr><td>market_open</td><td>0.6</td><td>Session opening prices</td></tr>
          <tr><td>macro_fearGreed</td><td>0.6</td><td>Sentiment index</td></tr>
          <tr><td>rss:reddit_crypto</td><td>0.5</td><td>r/CryptoCurrency community signal</td></tr>
          <tr><td>rss:reddit_btc</td><td>0.5</td><td>r/Bitcoin community signal</td></tr>
          <tr><td>dexscreener</td><td>0.2</td><td>Enrichment only</td></tr>
          <tr><td>Default (unlisted)</td><td>0.5</td><td>-</td></tr>
        </tbody>
      </table>
    </div>

    <div class="card">
      <h2 id="signals-similar">GET /signals/similar</h2>
      <p><em>Pro tier only.</em> Semantic nearest-neighbor search across all signals using BGE-M3 vector embeddings. Find signals related by meaning — not just matching keywords.</p>
      <h3>Query Parameters</h3>
      <table>
        <thead><tr><th>Param</th><th>Type</th><th>Description</th></tr></thead>
        <tbody>
          <tr><td><code>coin</code></td><td>string</td><td>Optional — filter results to this asset ticker</td></tr>
          <tr><td><code>q</code></td><td>string</td><td>Free-text query (defaults to coin name if omitted)</td></tr>
          <tr><td><code>k</code></td><td>int</td><td>Number of results (default 5, max 20)</td></tr>
        </tbody>
      </table>
      <pre><code set:html={similarResponse} /></pre>
    </div>

    <div class="card">
      <h2 id="narrative">GET /narrative/{'{coin}'}</h2>
      <p><em>Pro tier only.</em> Rolling narrative momentum for an asset — how many signals have fired, at what velocity, from how many independent sources, and whether a coordination pattern is detected.</p>
      <pre><code set:html={narrativeResponse} /></pre>
      <h3>Field Reference</h3>
      <table>
        <thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>
        <tbody>
          <tr><td><code>narrative_strength</code></td><td>float</td><td>0.0–1.0 weighted signal volume over 24h</td></tr>
          <tr><td><code>narrative_velocity</code></td><td>float</td><td>Rate of new signals per hour in 1h window</td></tr>
          <tr><td><code>narrative_decay_rate</code></td><td>float</td><td>How quickly signal volume is falling (0 = growing, 1 = cold)</td></tr>
          <tr><td><code>manipulation_probability</code></td><td>float</td><td>0.0–1.0. Elevated if ≥3 independent source types fire same direction within 5 min</td></tr>
          <tr><td><code>dominant_direction</code></td><td>string</td><td>bullish, bearish, or neutral (majority vote across window)</td></tr>
          <tr><td><code>direction_conviction</code></td><td>float</td><td>Fraction of signals matching dominant direction</td></tr>
          <tr><td><code>source_diversity</code></td><td>int</td><td>Number of distinct source types in 1h window</td></tr>
          <tr><td><code>signal_count_1h</code> / <code>4h</code> / <code>24h</code></td><td>int</td><td>Raw signal count per rolling window</td></tr>
        </tbody>
      </table>
    </div>

    <div class="card">
      <h2 id="manipulation">GET /manipulation/{'{coin}'}</h2>
      <p><em>Pro tier only.</em> Composite manipulation score for an asset. Combines social burst, coordinated pump, and liquidation cascade sub-scores.</p>
      <pre><code set:html={manipulationResponse} /></pre>
      <h3>Field Reference</h3>
      <table>
        <thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>
        <tbody>
          <tr><td><code>manipulation_score</code></td><td>float</td><td>Composite: social_burst×0.30 + coordinated_pump×0.45 + liq_cascade×0.25</td></tr>
          <tr><td><code>social_burst_score</code></td><td>float</td><td>Elevated if social signal count ≥5× 10-min baseline</td></tr>
          <tr><td><code>coordinated_pump_score</code></td><td>float</td><td>Elevated if ≥4 distinct source prefixes fire same direction in 10 min</td></tr>
          <tr><td><code>liquidation_cascade_score</code></td><td>float</td><td>Elevated if ≥3 liquidation signals for coin within 15 min</td></tr>
          <tr><td><code>flags</code></td><td>array</td><td>Active flags: social_burst, coordinated_pump, liquidation_cascade</td></tr>
        </tbody>
      </table>
    </div>

    <div class="card">
      <h2 id="trust">GET /trust/{'{coin}'}</h2>
      <p><em>Pro tier only.</em> Trust and scam scoring for a token ticker. Resolves the ticker to a contract address via DexScreener, then checks GoPlus Security for contract flags. Results are cached 6 hours per token address.</p>
      <pre><code set:html={trustResponse} /></pre>
      <h3>Field Reference</h3>
      <table>
        <thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>
        <tbody>
          <tr><td><code>trust_score</code></td><td>float</td><td>1.0 − rug_probability. Higher = safer.</td></tr>
          <tr><td><code>rug_probability</code></td><td>float</td><td>Composite: contract_risk×0.60 + liquidity_risk×0.20 + vol_liq_risk×0.10 + signal_history_risk×0.10</td></tr>
          <tr><td><code>contract_risk</code></td><td>float</td><td>GoPlus-derived. Sums: honeypot (+0.50), mintable (+0.20), owner_can_change_balance (+0.25), upgradeable_proxy (+0.10), high_sell_tax, insider_pct</td></tr>
          <tr><td><code>insider_pct</code></td><td>float</td><td>Creator + owner combined token holding percentage</td></tr>
          <tr><td><code>is_honeypot</code></td><td>bool</td><td>GoPlus honeypot flag — token cannot be sold</td></tr>
          <tr><td><code>is_mintable</code></td><td>bool</td><td>Contract can mint unlimited new supply</td></tr>
          <tr><td><code>sell_tax</code></td><td>float</td><td>Sell transaction tax (0.10 = 10%)</td></tr>
          <tr><td><code>low_liquidity</code></td><td>bool</td><td>True if liquidity_usd &lt; $50,000</td></tr>
          <tr><td><code>flags</code></td><td>array</td><td>Active contract risk flags from GoPlus</td></tr>
          <tr><td><code>data_source</code></td><td>string</td><td>goplus, dexscreener, signal_history_only, or none</td></tr>
        </tbody>
      </table>
    </div>

    <div class="card">
      <h2 id="errors">Error Responses</h2>
      <pre><code set:html={errorResponses} /></pre>
    </div>
  </section>
</BaseLayout>