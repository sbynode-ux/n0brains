---
import BaseLayout from "../layouts/BaseLayout.astro";
import MonoChip from "../components/MonoChip.astro";

/* ---------- Source taxonomy (lives here so it's editable in one place) ---------- */
const sources = [
  { chip: "TG",  title: "Telegram Channels",      desc: "WatcherGuru, ZachXBT Investigations, and more. Fastest signal source for hacks and breaking news." },
  { chip: "WH",  title: "Whale Tracking",         desc: "Hyperliquid leaderboard consensus. When top traders pile into the same side, we detect it." },
  { chip: "L/S", title: "L/S Ratios",             desc: "Bybit, Binance, and Gate.io positioning data. Detect extreme crowding before reversals." },
  { chip: "$",   title: "Funding & Liquidations", desc: "Binance funding extremes and real-time liquidation cascades via WebSocket." },
  { chip: "ETH", title: "On-Chain Flows",         desc: "Large ETH/BTC transfers to/from exchanges. Whale deposits = selling pressure." },
  { chip: "RSS", title: "News & RSS",             desc: "CoinDesk, Decrypt, CoinTelegraph, Reddit, and Trump's Truth Social. Keyword-filtered for crypto." },
  { chip: "CMC", title: "CMC & Macro",            desc: "CoinMarketCap price alerts, trending coins, Fear & Greed Index, DeFi TVL shifts." },
  { chip: "SR",  title: "S/R Levels",             desc: "Real-time support/resistance from Hyperliquid order book. Rejects trades near adverse levels." },
];

const proFeatures = [
  "600 requests / minute",
  "<strong>Real-time</strong> signals — no delay",
  "REST + WebSocket + Webhooks",
  "Full source &amp; channel detail",
  "Entry, SL &amp; TP levels",
  "Whale tracking &amp; on-chain flows",
  "Corroboration scoring",
  "Email support",
];
const freeFeatures = [
  "60 requests / minute",
  "15-minute signal delay",
  "REST API only",
  "Limited signal detail",
  "Community support",
];
---

<BaseLayout
  title="AI-Powered Crypto Signals API"
  description="10+ verified signal sources, machine-classified and cross-corroborated in real-time. REST API, WebSocket, and Webhooks built for autonomous trading agents. Free tier — no credit card."
  homepage={true}
  ogType="website"
>
  <style>
    .signals-page { max-width: 1100px; margin: 0 auto; padding: 0 1.5rem; }

    .hero-section { padding: 6rem 0 4rem; text-align: center; }
    .hero-section h1 {
      font-family: var(--font-display);
      font-size: clamp(3rem, 8vw, 5.5rem);
      font-weight: 400; line-height: 1; letter-spacing: 0.03em;
      margin-bottom: 1rem; color: var(--text-heading);
    }
    .hero-section h1 .accent { color: var(--accent); }
    .hero-section .lede {
      font-size: 1.1rem; color: var(--text); /* upgraded from --text-muted for AA */
      max-width: 600px; margin: 0 auto 2rem; line-height: 1.6;
    }
    .hero-section .lede strong { color: var(--text-heading); font-weight: 500; }

    .live-dot {
      display: inline-block; width: 8px; height: 8px;
      background: var(--accent); border-radius: 50%;
      margin-right: 8px; animation: pulse 2s infinite;
    }
    @keyframes pulse { 0%,100% { opacity: 1; } 50% { opacity: 0.3; } }

    .cta-row { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }
    .btn {
      display: inline-flex; align-items: center; gap: 0.5rem;
      padding: 0.85rem 1.6rem; border-radius: var(--radius-sm);
      font-family: var(--font-mono); font-size: 0.85rem; font-weight: 600;
      text-transform: uppercase; letter-spacing: 0.05em;
      transition: all 0.2s ease; cursor: pointer; border: none;
    }
    .btn-primary   { background: var(--accent); color: #000; }
    .btn-primary:hover   { background: #fff; }
    .btn-secondary { background: transparent; color: var(--text); border: 1px solid var(--border); }
    .btn-secondary:hover { border-color: var(--accent); color: var(--accent); }

    /* Sources */
    .sources-section, .pricing-section, .stats-section {
      padding: 4rem 0; border-top: 1px solid var(--border);
    }
    .sources-section h2, .pricing-section h2, .stats-section h2 {
      font-family: var(--font-display);
      font-size: 2.4rem; font-weight: 400;
      color: var(--text-heading); letter-spacing: 0.04em;
      margin-bottom: 0.5rem;
    }
    .pricing-section h2 { text-align: center; }
    .sources-section .subtitle, .pricing-section .subtitle {
      color: var(--text); margin-bottom: 2rem;
    }
    .pricing-section .subtitle { text-align: center; margin-bottom: 2.5rem; }

    .sources-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
      gap: 1px; background: var(--border);
    }
    .source-card {
      background: var(--bg-alt); padding: 1.5rem;
      transition: background 0.2s ease;
    }
    .source-card:hover { background: var(--bg-hover); }
    .source-card .chip-row { margin-bottom: 0.75rem; }
    .source-card h3 { font-size: 0.95rem; font-weight: 700; margin-bottom: 0.25rem; color: var(--text-heading); }
    .source-card p  { font-size: 0.85rem; color: var(--text); line-height: 1.55; }

    /* Pricing */
    .pricing-grid {
      display: grid; grid-template-columns: 1fr 1fr;
      gap: 1.5rem; max-width: 750px; margin: 0 auto;
    }
    @media (max-width: 640px) { .pricing-grid { grid-template-columns: 1fr; } }

    .pricing-card {
      background: var(--bg-alt);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 2rem; position: relative;
    }
    .pricing-card.featured { border-color: var(--accent); }
    .pricing-card .badge {
      position: absolute; top: -1px; right: -1px;
      background: var(--accent); color: #000;
      font-family: var(--font-mono); font-size: 0.65rem; font-weight: 700;
      padding: 0.25rem 0.75rem; text-transform: uppercase; letter-spacing: 0.05em;
      border-radius: 0 var(--radius-sm) 0 var(--radius);
    }
    .pricing-card .tier-name {
      font-family: var(--font-mono); font-size: 0.7rem;
      text-transform: uppercase; letter-spacing: 0.15em;
      color: var(--text); margin-bottom: 0.5rem;
    }
    .pricing-card.featured .tier-name { color: var(--accent); }
    .pricing-card .price {
      font-size: 2.5rem; font-weight: 800; line-height: 1; margin-bottom: 0.25rem;
      color: var(--text-heading);
    }
    .pricing-card .price .period { font-size: 0.9rem; font-weight: 400; color: var(--text); }
    .pricing-card .desc { font-size: 0.9rem; color: var(--text); margin-bottom: 1.5rem; }
    .pricing-card ul { list-style: none; padding: 0; margin-bottom: 1.5rem; }
    .pricing-card li {
      display: flex; align-items: flex-start; gap: 0.5rem;
      font-size: 0.9rem; margin-bottom: 0.6rem; color: var(--text);
    }
    .pricing-card li .check { color: var(--accent); flex-shrink: 0; margin-top: 2px; }

    /* Stats */
    .stats-grid {
      display: grid; grid-template-columns: repeat(4, 1fr);
      gap: 1px; background: var(--border);
    }
    @media (max-width: 640px) { .stats-grid { grid-template-columns: repeat(2, 1fr); } }
    .stat-card { background: var(--bg-alt); padding: 1.5rem; }
    .stat-card .label {
      font-family: var(--font-mono); font-size: 0.65rem;
      text-transform: uppercase; letter-spacing: 0.1em;
      color: var(--text); margin-bottom: 0.5rem;
    }
    .stat-card .value { font-size: 1.8rem; font-weight: 800; color: var(--text-heading); }
    .stat-card .value.green { color: var(--accent); }
    .stat-card .value.blue  { color: #5c7cfa; }
  </style>

  <div class="signals-page">
    <!-- Hero -->
    <section class="hero-section" id="hero">
      <p style="font-family: var(--font-mono); font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.2em; color: var(--accent); margin-bottom: 1.5rem;">
        <span class="live-dot"></span>Live Signal Feed Active
      </p>
      <h1>AI-POWERED<br /><span class="accent">CRYPTO</span><br />INTELLIGENCE</h1>
      <p class="lede">
        10+ verified sources. Machine-classified, backtested, and delivered via <strong>REST API</strong>, <strong>WebSocket</strong>, or <strong>Webhook</strong>. Built for traders who execute.
      </p>
      <div class="cta-row">
        <a href="/signup" class="btn btn-primary">Start Free →</a>
        <a href="/docs/" class="btn btn-secondary">API Docs ↗</a>
      </div>
    </section>

    <!-- Sources -->
    <section class="sources-section">
      <h2>10+ Signal Sources</h2>
      <p class="subtitle">Aggregated, classified, and corroborated in real-time.</p>
      <div class="sources-grid">
        {sources.map((s) => (
          <div class="source-card">
            <div class="chip-row"><MonoChip text={s.chip} /></div>
            <h3>{s.title}</h3>
            <p>{s.desc}</p>
          </div>
        ))}
      </div>
    </section>

    <!-- Pricing -->
    <section class="pricing-section" id="pricing">
      <h2>Choose your tier.</h2>
      <p class="subtitle">Start free. Upgrade to Pro for real-time signals.</p>
      <div class="pricing-grid">
        <div class="pricing-card">
          <div class="tier-name">Free</div>
          <div class="price">$0<span class="period">/mo</span></div>
          <p class="desc">For evaluation and light usage.</p>
          <ul>
            {freeFeatures.map((f) => (
              <li><span class="check" aria-hidden="true">✓</span><span set:html={f}></span></li>
            ))}
          </ul>
          <a href="/signup" class="btn btn-secondary" style="width: 100%; justify-content: center;">Start Free</a>
        </div>

        <div class="pricing-card featured">
          <div class="badge">Popular</div>
          <div class="tier-name">Pro</div>
          <div class="price">$49.99<span class="period">/mo</span></div>
          <p class="desc">For active traders. Everything you need.</p>
          <ul>
            {proFeatures.map((f) => (
              <li><span class="check" aria-hidden="true">✓</span><span set:html={f}></span></li>
            ))}
          </ul>
          <button data-checkout="pro" class="btn btn-primary" style="width: 100%; justify-content: center;">Get Pro</button>
        </div>
      </div>
    </section>

    <!-- Stats -->
    <section class="stats-section">
      <h2>30-Day Track Record</h2>
      <div id="home-stats-widget">
        <div class="stats-grid">
          <div class="stat-card"><div class="label">Win Rate</div><div class="value green" id="stat-winrate">—</div></div>
          <div class="stat-card"><div class="label">Total Signals</div><div class="value" id="stat-total">—</div></div>
          <div class="stat-card"><div class="label">Avg PnL</div><div class="value" id="stat-pnl">—</div></div>
          <div class="stat-card"><div class="label">Sources</div><div class="value blue" id="stat-sources">13</div></div>
        </div>
      </div>
    </section>
  </div>
</BaseLayout>

<script>
  const API_BASE = window.location.hostname === '127.0.0.1' || window.location.hostname === 'localhost'
    ? 'http://127.0.0.1:8000'
    : 'https://api.n0brains.com';

  async function loadStats() {
    try {
      const resp = await fetch(`${API_BASE}/performance/public`);
      if (!resp.ok) return;
      const data = await resp.json();
      if (data.win_rate != null) document.getElementById('stat-winrate').textContent = (data.win_rate * 100).toFixed(1) + '%';
      if (data.total_signals)    document.getElementById('stat-total').textContent   = data.total_signals.toLocaleString();
      if (data.avg_pnl != null)  document.getElementById('stat-pnl').textContent     = (data.avg_pnl >= 0 ? '+' : '') + data.avg_pnl.toFixed(2) + '%';
      if (data.sources)          document.getElementById('stat-sources').textContent = data.sources;
    } catch (_) { /* dashes remain */ }
  }

  function initCheckoutButtons() {
    document.querySelectorAll('[data-checkout]').forEach((btn) => {
      btn.addEventListener('click', (e) => {
        e.preventDefault();
        const tier  = (btn as HTMLElement).dataset.checkout;
        const apiKey = localStorage.getItem('api_key');
        const token  = localStorage.getItem('auth_token');
        if (!apiKey && !token) {
          window.location.href = '/signup?redirect=checkout&tier=' + encodeURIComponent(tier);
          return;
        }
        const headers: Record<string, string> = { 'Content-Type': 'application/json' };
        if (token) headers['Authorization'] = 'Bearer ' + token;
        else if (apiKey) headers['X-API-Key'] = apiKey;
        fetch(API_BASE + '/billing/checkout', {
          method: 'POST', headers,
          body: JSON.stringify({ price_id: tier }),
        }).then((r) => {
          if (r.status === 401) { window.location.href = '/login?redirect=checkout&tier=' + encodeURIComponent(tier); return; }
          return r.json();
        }).then((data) => {
          if (data && data.url) window.location.href = data.url;
        }).catch((err) => console.error('Checkout error:', err));
      });
    });
  }

  loadStats();
  initCheckoutButtons();
</script>
